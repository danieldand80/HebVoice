# Quick Start - Быстрый запуск

## Шаг 1: Проверка API ключа

1. Получите API ключ от Google AI Studio:
   - Перейдите на https://aistudio.google.com/app/apikey
   - Нажмите "Create API Key"
   - Скопируйте ключ

2. Создайте файл `.env` в корне проекта:
```env
GOOGLE_API_KEY=ваш_api_ключ_здесь
PORT=8000
```

## Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 3: Тест API (рекомендуется!)

Перед запуском приложения протестируйте, что API работает:

```bash
cd backend
python test_gemini_api.py
```

Вы должны увидеть:
```
✓ API Key found
✓ Client initialized
✓ Generating image...
✓ Image found in response
✓ Image saved to test_output.png
🎉 All tests passed!
```

Если тесты провалились, проверьте:
- ❌ Правильно ли указан GOOGLE_API_KEY в .env
- ❌ Есть ли доступ к интернету
- ❌ Не заблокирован ли API в вашей стране

## Шаг 4: Запуск приложения

```bash
cd backend
python main.py
```

Откройте браузер: http://localhost:8000

## Что исправлено в этом обновлении:

### ✅ Исправлена ошибка `400 INVALID_ARGUMENT`
- **Проблема**: Старый код пытался использовать `response_mime_type`, который не поддерживается для генерации изображений
- **Решение**: Удалили `response_mime_type`, используем только `response_modalities=['IMAGE']`

### ✅ Исправлена ошибка `PIL.UnidentifiedImageError`
- **Проблема**: Передавались пустые байты изображения (size: 0)
- **Решение**: Добавлена валидация:
  - Проверка размера файла перед обработкой
  - Валидация что файл - это действительно изображение
  - Детальное логирование для отладки

### ✅ Исправлена проверка переменных окружения
- **Проблема**: Код проверял `GOOGLE_PROJECT_ID`, но нужен только `GOOGLE_API_KEY`
- **Решение**: Изменили проверку на `GOOGLE_API_KEY`

### ✅ Улучшено логирование
- Добавлены подробные DEBUG логи для отслеживания:
  - Размер загруженного файла
  - Формат и размеры изображения
  - Каждый шаг обработки

## Использование

### Режим 1: Генерация из текста (text2img)
1. Оставьте поле "Upload image" пустым
2. Введите промпт: "красные кроссовки на белом фоне"
3. Выберите формат (16:9 или 9:16)
4. Нажмите "Generate"

### Режим 2: Редактирование изображения (img2img)
1. Загрузите изображение продукта
2. Введите промпт: "change background to modern shop"
3. Выберите формат (16:9 или 9:16)
4. Нажмите "Generate"

### Добавление текста
1. После генерации изображения введите текст на иврите
2. Выберите позицию из предложенных
3. Настройте размер шрифта и цвета
4. Нажмите "Add Text"
5. Скачайте готовое изображение

## Отладка

Если что-то не работает, проверьте логи в терминале. Теперь там подробная информация о каждом шаге.

**Пример нормальных логов:**
```
[Google AI Studio] API configured successfully
[DEBUG] Uploaded image bytes size: 234567
[DEBUG] Image validated: (1024, 768), mode: RGB
[Nano Banana] Editing image with prompt: make it better
[Nano Banana] PIL Image loaded: (1024, 768), mode: RGB
[Nano Banana] Image edited successfully! Size: 345678 bytes
```

**Пример ошибки:**
```
[DEBUG] Uploaded image bytes size: 0
ERROR: Image bytes are empty! Cannot edit empty image.
```
→ Решение: Проверьте, что файл загружается корректно в браузере

## Документация API

- 📖 Официальная документация Gemini: https://ai.google.dev/gemini-api/docs/image-generation
- 🔑 Получить API ключ: https://aistudio.google.com/app/apikey
- 💰 Цены: https://ai.google.dev/pricing

## Поддержка

Если проблема сохраняется:
1. Запустите `python backend/test_gemini_api.py` и пришлите вывод
2. Проверьте логи сервера при ошибке
3. Убедитесь, что используете актуальную версию google-genai SDK



