[phases.setup]
nixPkgs = ['python312', 'freetype', 'fontconfig', 'liberation_ttf', 'dejavu_fonts']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Fonts installed successfully"']

[start]
cmd = 'uvicorn backend.main:app --host 0.0.0.0 --port $PORT'

